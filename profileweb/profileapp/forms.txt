suppose if userprofile  is my model

class UserProfile(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    bio = models.TextField(blank=True)
    profile_picture = models.ImageField(upload_to='profile_pics', blank=True, null=True)


->Then i can create a form in forms .py

    class CreateProfile():
        model=UserProfile
        fields=[here mention the required field from the models ] # i can add __all__ if i need all fields

while using the form and for validating the data i have to use under if

def create_userprofile():
    if request.method=="POST":
        form=forms.CreateProfile(request.POST,request.FILES)# this part does the validation job
        if forms.is_valid():
            form.save() # this part is for saving the form if it is valid to the database

    else:
        forms=forms.CreateProfile()# this part again renders the create form page
    return render(request,"htmlfile",{"form":forms})# ,mention the instance you ve used above

-> if the form in case contains the files to be uploaded you need to add request.FILES

